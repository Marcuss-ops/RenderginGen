# Architecture conformance baseline (ratchet — may only shrink).
# Format: <rule>|<workspace-relative-file>. Regenerate explicitly with
# UPDATE_CONFORMANCE_BASELINE=1 go test ./internal/architecture/...
